{
  "name": "project-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "npm --prefix ./backend run dev:local",
    "start:frontend": "npm --prefix ./frontend run dev:local"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.11",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
